Replace the `btn-navy` class on the Header's "Get Quote" button with `btn-gold` so it renders the shiny metallic gold gradient button instead of the solid navy one.

### Files
- `src/components/Header.tsx` — change the desktop "Get Quote" `<Link>` class from `btn-base btn-navy` to `btn-base btn-gold`.

### Out of scope
- Mobile menu "Get Quote" button (currently `btn-base btn-navy` inside the mobile drawer) — leave as-is unless requested.
- Any other buttons or styles on the site.